const state = {
  dark: false
}

const mutations = {
  SET_DARK(state, value) {
    state.dark = value
  }
}

const actions = {}

export default {
  spacename: true,
  state,
  mutations,
  actions
}
